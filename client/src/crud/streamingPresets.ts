// Copyright Schulich Racing, FSAE
// Written by Justin Tijunelis

import { StreamingPreset } from "state";

export const getStreamingPresets = (thingId: string) => {
  return new Promise<StreamingPreset[]>((resolve, reject) => {});
};

export const postStreamingPreset = (streamingPreset: StreamingPreset) => {
  return new Promise<void>((resolve, reject) => {});
};

export const putStreamingPreset = (streamingPreset: StreamingPreset) => {
  return new Promise<void>((resolve, reject) => {});
};

export const deleteStreamingPreset = (streamingPresetId: string) => {
  return new Promise<void>((resolve, reject) => {});
};
